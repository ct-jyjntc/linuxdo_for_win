<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="LinuxDo.Features.Compose.DraftsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="20,12" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="草稿" />
            <Button HorizontalAlignment="Right" Content="从服务器同步" Click="Sync_Click" />
        </Grid>
        <Grid Grid.Row="1">
            <ProgressRing x:Name="LoadingRing" Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center" IsActive="False" Visibility="Collapsed" />
            <ListView x:Name="DraftList" IsItemClickEnabled="True" ItemClick="DraftList_ItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="12,10" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Spacing="4">
                                <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                                <TextBlock FontSize="12" Opacity="0.65" Text="{Binding Subtitle}" TextTrimming="CharacterEllipsis" MaxLines="2" />
                                <TextBlock FontSize="11" Opacity="0.5" Text="{Binding Badge}" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="删除" Click="Delete_Click" Tag="{Binding}" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock x:Name="EmptyText" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" Text="暂无草稿" Visibility="Collapsed" />
        </Grid>
    </Grid>
</Page>
